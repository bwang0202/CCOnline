package util;

/**
 * Lambda interface.
 *
 */
public interface ILambda<R,P> {
	/**
	 * Apply the lambda.
	 *
	 * @param param lambda-specific parameter
	 * @return lambda-specific return value
	 */
	R apply(@SuppressWarnings("unchecked") P... param);
}
