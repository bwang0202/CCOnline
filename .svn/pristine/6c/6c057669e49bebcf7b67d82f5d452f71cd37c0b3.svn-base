package server.view;

/**
 * Adapter which make the connections from view to model.
 * @author Fabio-CSM
 *
 */
public interface IServerViewToModelAdapter {

	/**
	 * Send a status message to the client
	 * @param msg Number of the status message.
	 */
	void sendStatusMsg(Integer msg);
	
}
